<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.commonsurvey.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CommonSurvey">
        <activity android:name=".CommonGroup.Model.GeneralWork"></activity>
        <activity android:name=".CommonGroup.Model.MachineryGroup" />
        <activity android:name=".CommonGroup.Model.HerbicideGroup" />
        <activity android:name=".CommonGroup.Model.WeedingGroup" />
        <activity android:name=".CommonGroup.Model.PesticideGroup" />
        <activity android:name=".CommonGroup.Model.FertilizerGroup" />
        <activity android:name=".CommonGroup.CommonGroupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>